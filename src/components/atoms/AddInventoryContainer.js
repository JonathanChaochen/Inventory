import styled from 'styled-components';

const AddInventoryContainer = styled.div`
  display: flex;
  display-direction: column;
`;
AddInventoryContainer.displayName = 'AddInventoryContainer';

export default AddInventoryContainer;
